# Misc

Small utilities.

## User-invoked

Reachable only when you type them (`disable-model-invocation: true`).

- **[session](./session/SKILL.md)** — `/session`. Copy the current Claude Code session id to the clipboard (`wl-copy`/`xclip`/`xsel`/`pbcopy`) and confirm.
